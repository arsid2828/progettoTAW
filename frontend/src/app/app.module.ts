import { NgModule } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';

@NgModule({
  declarations: [
    // Rimuovi AppComponent da qui
  ],
  imports: [
    BrowserModule
  ],
  bootstrap: [] // Lascia vuoto se usi un componente standalone
})
export class AppModule { }